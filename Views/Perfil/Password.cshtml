@{
    ViewData["Title"] = "Cambiar contraseña";
}
<h2>@ViewData["Title"]</h2>

@if (TempData["Ok"] != null) {
    <div class="alert alert-success">@TempData["Ok"]</div>
}

<form asp-action="Password" method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="form-group mb-3">
        <label>Contraseña actual</label>
        <input name="Actual" type="password" class="form-control" />
    </div>

    <div class="form-group mb-3">
        <label>Nueva contraseña</label>
        <input name="Nueva" type="password" class="form-control" />
    </div>

    <div class="form-group mb-4">
        <label>Repetir nueva contraseña</label>
        <input name="Repetir" type="password" class="form-control" />
    </div>

    <button class="btn btn-primary" type="submit">Actualizar</button>
    <a asp-action="Datos" class="btn btn-secondary">Volver</a>
</form>
